## Building the l2geth Docker Image

Run the build.sh script. This clones the optimism monorepo and builds the official l2geth Dockerfile. The `IMAGE_TAG` envar configures the image tag of the built iamge.
